name: camin
region: eu-central-1
language: ts
cloudProvider: genezio
scripts:
  preBackendDeploy: npm install
  preFrontendDeploy: npm run install-prod-sdk && npm install && npm run build
frontend:
  path: client/dist
  subdomain: fuchsia-awkward-baboon
packageManager: npm
workspace:
  backend: server
  frontend: client
