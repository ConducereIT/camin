name: camin-runtime
region: eu-central-1
yamlVersion: 2
backend:
  path: server
  language:
    name: ts
    packageManager: npm
  scripts:
    deploy: npm install
frontend:
  path: client
  sdk:
    language: ts
  publish: dist
  scripts:
    deploy:
      - npm install
      - npm i @genezio/auth@latest
      - npm install @genezio-sdk/camin-runtime
      - npm run build
  subdomain: aquamarine-minimal-crocodile
